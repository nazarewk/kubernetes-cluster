#!/bin/sh
# Author: Krzysztof Nazarewski <nazarewk@gmail.com>
# Purpose: activates Python virtualenv and sets common shell variables

. ${0%/*}/vars
[ -f "$PDIR/.env" ] && . $PDIR/.env/bin/activate || . ensure-virtualenv
