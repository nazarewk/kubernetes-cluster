#!/bin/sh
cd $(dirname "$(readlink -f "$0")")/..
export KUBECONFIG=artifacts/admin.conf
[ -f artifacts/kubectl ] && chmod +x artifacts/kubectl
[ -f artifacts/kubectl ] && kubectl=artifacts/kubectl || kubectl=/usr/bin/kubectl
${kubectl} "$@"