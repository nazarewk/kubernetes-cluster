#!/usr/bin/env bash
base_dir=$(readlink -f $(dirname "$(readlink -f "$0")")/..)
set -e

${base_dir}/bin/pull
${base_dir}/bin/setup-packages
source ${base_dir}/bin/activate
${base_dir}/bin/setup-cluster "$@"
